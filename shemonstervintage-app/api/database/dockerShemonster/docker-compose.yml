services:
  db:
    image: mysql:8.0
    container_name: shemonstervintage-db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: rootpass
      MYSQL_DATABASE: shemonstervintage
      MYSQL_USER: wishlist_user
      MYSQL_PASSWORD: wishlist_pass
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data:
